apiVersion: 1

providers:
  - name: assignment-platform
    folder: Assignment Platform
    type: file
    disableDeletion: false
    updateIntervalSeconds: 15
    options:
      path: /var/lib/grafana/dashboards
